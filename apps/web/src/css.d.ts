// Ambient module declaration for global CSS side-effect imports (e.g.
// `import './globals.css'` in app/layout.tsx). Next's bundler resolves these
// at build time; TypeScript needs this to type-check the import itself.
declare module '*.css';
