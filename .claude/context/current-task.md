# Current task

None active.

The `/impeccable critique apps/web` backlog (2026-09-21) is now fully worked
through: CR-103 (Dialog/ConfirmDialog/Toast), CR-104 (RideSummaryWidget),
CR-105 (sticky mobile registration CTA), CR-106 (cabinet/header icons), and
CR-107 (the synthesized "Quiet Instrument" visual direction — tokens,
`Wordmark`, glass cover-photo/sticky-bar surfaces, bolder route line). Full
detail on all five: `docs/changelog.md`'s CR-103 through CR-107 entries;
current snapshot: `.claude/context/project-state.md`.

Only P3 remains from the original critique, unscheduled and not authorized:
`docs/design.md` §9's component inventory still lists `Tabs`/`Sheet`/
`Select`/`Checkbox`/`RadioGroup`/`DatePicker`/`Pagination` as if built in
`packages/ui`, but they don't exist yet (`RideFilters.tsx` hand-vendors a
workaround for the missing `Select`, per KI-020). A fresh session should
not start on this without the user's own «внедряй» — reference the two
critique snapshot files if it comes up:
`apps/web/.impeccable/critique/2026-09-21T15-54-48Z__apps-web.md` and
`...T16-23-05Z__apps-web.md`.
