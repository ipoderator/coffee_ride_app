// packages/ui — reusable UI, shared between the organizer and participant
// cabinets (`.claude/rules/architecture.md`, ADR-009).
//
// Intentionally empty, same "tooling first, content when there's a real
// consumer" discipline packages/db used for its zero domain tables (CR-004):
// design tokens (CR-063), the Russian formatter/terminology module (CR-064),
// and the first shared components (`MetricTile`, `StatusBadge`, ...,
// CR-065/CR-066) all land before apps/web has a real screen to put them on
// (CR-011). Read `docs/design.md` before adding the first real export here.
export {};
